#include<stdio.h>

int main(){
  int integer;
  float decimal;
  double doub;
  char charactor;

  printf("size of int is: %d\n", sizeof(integer));
  printf("size of float is: %d\n", sizeof(decimal));
  printf("size of double is: %d\n", sizeof(doub));
  printf("size of char is: %d\n", sizeof(charactor));
}